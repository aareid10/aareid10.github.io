export default class socialEngine {
  /**
   * The socialEngine Class.
   * Property (constructor) in socialEngine which creates the class object.
   * @classdesc This is a description of the socialEngine class.
   * @member {Function} socialEngine
   * @access public
   * @constructs
   * @requires {Module/Class}
   * @param {null} None no parameter
   * @example
   * new socialEngine()
   * @returns {Class} socialEngine
   * @property {object} socialEngine.state - socialEngine state object
   * @author Alex Reid <https://github.com/aareid10>
   * @see {@link http://github.com|ProjectRepo}
   * @since 1.0.0
   */
  constructor() {
    this.state = {}
  }
}
