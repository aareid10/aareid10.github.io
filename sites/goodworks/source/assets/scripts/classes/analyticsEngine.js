export default class analyticsEngine {
  /**
   * The analyticsEngine Class.
   * Property (constructor) in analyticsEngine which creates the class object.
   * @classdesc This is a description of the analyticsEngine class.
   * @member {Function} analyticsEngine
   * @access public
   * @constructs
   * @requires {Module/Class}
   * @param {null} None no parameter
   * @example
   * new analyticsEngine()
   * @returns {Class} analyticsEngine
   * @property {object} analyticsEngine.state - analyticsEngine state object
   * @author Alex Reid <https://github.com/aareid10>
   * @see {@link http://github.com|ProjectRepo}
   * @since 1.0.0
   */
  constructor() {
    this.state = {}
  }
}
