import socialEngine from '@classes/socialEngine';

beforeAll(() => {})
beforeEach(() => {})

describe('The socialEngine Class', () => {
  it('stub', () => {
    expect(true).toBe(true)
  })
})

afterEach(() => {})
afterAll(() => {})
