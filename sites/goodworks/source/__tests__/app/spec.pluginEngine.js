import pluginEngine from '@classes/pluginEngine';

beforeAll(() => {})
beforeEach(() => {})

describe('The pluginEngine Class', () => {
  it('stub', () => {
    expect(true).toBe(true)
  })
})

afterEach(() => {})
afterAll(() => {})
