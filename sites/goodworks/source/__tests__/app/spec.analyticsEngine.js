import analyticsEngine from '@classes/analyticsEngine';

beforeAll(() => {})
beforeEach(() => {})

describe('The analyticsEngine Class', () => {
  it('stub', () => {
    expect(true).toBe(true)
  })
})

afterEach(() => {})
afterAll(() => {})
