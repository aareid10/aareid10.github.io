import interactiveEngine from '@classes/interactiveEngine';

beforeAll(() => {})
beforeEach(() => {})

describe('The interactiveEngine Class', () => {
  it('stub', () => {
    expect(true).toBe(true)
  })
})

afterEach(() => {})
afterAll(() => {})
