// TODO: Uncheck all checkboxex on check of any checkbox | document.querySelector('.custom-label input').checked
// TODO: on check checkbox look at data of parent card and submit vote

export default class interactiveEngine {
  /**
   * The interactiveEngine Class.
   * Property (constructor) in interactiveEngine which creates the class object.
   * @classdesc This is a description of the interactiveEngine class.
   * @member {Function} interactiveEngine
   * @access public
   * @constructs
   * @requires {Module/Class}
   * @param {null} None no parameter
   * @example
   * new interactiveEngine()
   * @returns {Class} interactiveEngine
   * @property {object} interactiveEngine.state - interactiveEngine state object
   * @author Alex Reid <https://github.com/aareid10>
   * @see {@link http://github.com|ProjectRepo}
   * @since 1.0.0
   */
  constructor() {
    this.state = {}
  }
}
